"""
예제 13-7: 무한 루프 — while 루프에서 증감 누락
=================================================
흔한 오류: while 루프 안에서 카운터 변수를 변경하지 않아
루프가 영원히 끝나지 않는 경우

⚠️ 주의: 이 코드를 실행하면 무한 루프에 빠집니다!
         Ctrl+C를 눌러 강제 종료하세요.
"""

# 버그가 있는 코드: 카운터 증가 구문 누락
print("1부터 5까지 출력합니다:")

count = 1
while count <= 5:
    print(f"  현재 값: {count}")
    # count += 1  ← 이 줄이 빠져 있어서 count가 계속 1!
    # 무한 루프 방지를 위해 안전장치 추가 (데모용)
    # 실제 버그에서는 이 안전장치가 없습니다
    import sys
    print("  ⚠️ 무한 루프 감지! (데모를 위해 강제 종료합니다)")
    sys.exit(1)
